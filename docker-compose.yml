version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  feedo-backend:
    image: itsshas/feedo-backend:latest
    container_name: feedo-backend
    restart: always
    ports:
      - "8040:8040"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - lancedb_data:/app/lancedb_data

  feedo-p2p:
    image: itsshas/feedo-p2p:latest
    container_name: feedo-p2p
    restart: always
    ports:
      - "4001:4001/udp"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - PYTHON_API_URL=http://feedo-backend:8040
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      db:
        condition: service_healthy
      feedo-backend:
        condition: service_started
    volumes:
      - rust_db_data:/app/db

  feedo-frontend:
    image: itsshas/feedo-client:latest
    build:
      context: ./feedo_search_ui
    container_name: feedo-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - feedo-backend

  feedo-algo:
    image: itsshas/feedo-algo:latest
    build:
      context: ./protocol/algo-node
    container_name: feedo-algo
    restart: always
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
  rust_db_data:
  lancedb_data:
